# AGENTS

このファイルの配下（リポジトリ全体）に適用されるルールです。追加の AGENTS.md が存在する場合はそちらを優先してください。

1. **README と UI 仕様の同期**: 機能や UI の挙動を変更・追加した場合は、README（および関連ドキュメント）も必ず更新し、変更点を日本語で説明してください。
2. **依存関係の慎重な追加**: 既存の Electron + プレーン JavaScript/HTML/CSS 構成を維持し、外部ライブラリを追加したい場合は最小限に抑え、理由をコードコメントかコミットメッセージで明示してください。
3. **UI/コードスタイル**: レンダラーの UI はシンプルで軽量なスタイルを保ち、既存の CSS 設計（パネルレイアウト、タイポグラフィ）に合わせてください。JavaScript はモジュール化よりも可読性を優先し、`renderer.js` 内の既存パターンに従ってください。
4. **ログとエラー表示**: 新しい処理でユーザーに通知が必要な場合は、既存のステータス表示エリアやブラウザコンソールを活用し、不要な `alert` などのブロッキング UI は使用しないでください。
5. **日本語でのコミュニケーション**: コメント、ドキュメント、PR 説明など人が読む文章は日本語で記載してください（コード内の識別子や API 名は英語で構いません）。
